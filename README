# A Server-Side Persistent TiddlyWiki

This project is a demo of a server-side persistence layer for https://en.wikipedia.org/wiki/TiddlyWiki

## build and deploy

```
polly build Dockerfile
kubectl apply -f kubernetes
```
